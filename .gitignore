# Dependencies
node_modules/

# Build output (regenerated by build.js / vite build)
dist/

# Admin server runtime data (regenerated from seed.json via /api/dev/reset)
admin/server/data/db.json
admin/server/data/ingestion.db
admin/server/data/ingestion.db-shm
admin/server/data/ingestion.db-wal

# Editor/OS
.DS_Store

# Claude Code local settings (machine-specific permission allowlist)
.claude/settings.local.json
